/* eslint-disable */
/*
// STRINGS
const name = 'Genesis';
const middle = "Britanico";
const last = `Gabiola`;

const sentence = "shes's so \"cool\"";
const sentence2 = `shes's so "cool"`;

const song = `Ohhh

ya

I like
pizza`;

const hello = 'hello my name is ' + name + ". Nice to meet you";

let hello2 = 'hello my name is ';
hello2 = hello2 + name;
hello2 = hello2 + ' Nice to meet you';

const hello = `hello my name is ${name}. Nice to meet you. I am ${1 + 100} years old.`;

const html = `
  <div>
    <h2>${name}</h2>
    <p>${hello}</p>
  </div>
`;

document.body.innerHTML = html;
*/

/*
// NUMBERS
const age = 100.5;

const a = 10;
const b = 20;

const smarties = 20;
const kids = 3;
const eachKidGets = Math.floor(smarties / kids);
const dadGets = smarties % kids; // 2
console.log(`Each kids gets ${eachKidGets}`); // 6

const price = 1034;
*/

/*
// OBJECTS
const person = {
  first: 'Genesis',
  last: 'Gabiola',
  age: 27
};

person.first; // Genesis
person.age; // 27
*/

/*
// NULL and UNDEFINED
let dog;
console.log(dog); // undefined
dog = 'snickers';


let somethingUndefined;
const somethingNull = null;

const cher = {
  first: 'cher'
};

const teller = {
  first: 'Raymond',
  last: 'Teller'
}

teller.first = 'Teller';
teller.last = null;

teller.last; // null
cher.last; // undefined
*/


// BOOLEANS and EQUALITY
let isDrawing = false;
let age = 18;
const ofAge = age > 19;
console.log(ofAge); // false

age = 100;
let age2 = 100;


10 === 10 // true
'10' == 10 // true
'10' === 10 // false
